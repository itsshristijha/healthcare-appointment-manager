server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # Single-page app: any unknown path falls back to index.html so
  # react-router client-side routes work on a hard refresh.
  location / {
    try_files $uri $uri/ /index.html;
  }
}
